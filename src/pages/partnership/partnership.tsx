import { FC } from 'react';

import stylesPartnership from './partnership.module.scss';

const Partnership: FC = (): JSX.Element => {
  return <div className={stylesPartnership.title}>Partnership</div>;
};

export default Partnership;
